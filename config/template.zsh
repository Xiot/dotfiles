source "$DOTFILE_ROOT/config/personal.zsh"
source "$HOME/.antidote/antidote.zsh"

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

antidote load
